---
title: HTML
createTime: 2025/10/03 13:41:43
permalink: /WebStack/5x32ihw8/
---

## 简介

HTML（HyperText Markup Language）是用于创建网页的标准标记语言。

## 基本结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>页面标题</title>
    </head>
    <body>
        <h1>欢迎来到我的网站</h1>
        <p>这是一个段落。</p>
    </body>
</html>
```

## DOCTYPE声明
DOCTYPE是 `document type` 的缩写。 `<!DOCTYPE html>`是 HTML5 的声明，位于文档的最前面，处于标签之前。

这是网页必备的组成部分，避免了浏览器的怪异模式。

## html

定义 HTML 文档，所有其他的HTML元素都要包裹在 `html` 标签中

```html
<!DOCTYPE html>
<html lang="zh-CN">
    ...
</html>
```
`lang`用于声明网页的主要语言，帮助浏览器、搜索引擎等正确处理页面内容。
- `en` 代表英语
- `zh-CN` 代表中文

## head

head标签用于定义文档的头部，文档的头部描述了文档的各种属性和信息，包括文档的标题、在 web 中的位置以及和其他文档的关系等。绝大多数文档头部包含的数据都不会作为内容展示。

```html
<!DOCTYPE html>
<html>
    <head>...</head>
</html>
```

head标签中包含的标签有：

### title 标签

`<title>` 是 `<head>` 标签中唯一必要的元素，它定义文档的标题，显示在浏览器窗口的标题栏或状态栏上。

```html
<!DOCTYPE html>
<html>
    <head>
        <title>这是标题</title>
    </head>
</html>
```

### meta 标签

meta标签用来描述一个HTML网页文档的属性、关键词等，例如：`charset="utf-8"`，一些常用的meta设置如下：

```html
<!DOCTYPE html>
<html>
    <head>
        <!-- 字符编码声明 -->
        <meta charset="UTF-8">

        <!-- 视口设置，响应式设计必备 -->
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        <!-- 网页兼容性设置 -->
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        
        <!-- 网页描述 -->
        <meta name="description" content="这是一个关于HTML head标签详解的示例页面">
        
        <!-- 网页关键词 -->
        <meta name="keywords" content="HTML, head标签, 元数据, SEO, 网页开发">
        
        <!-- 作者信息 -->
        <meta name="author" content="网页开发者">
        
        <!-- 版权信息 -->
        <meta name="copyright" content="版权所有">
    </head>
</html>
```

## body

body 元素定义文档的主体。

body元素包含文档的所有内容，比如文本、超链接、图像、表格和列表等

它会直接在页面中显示出来，也就是用户可以直观看到的内容。

```html
<!DOCTYPE html>
<html>
    <head>
        ...
    </head>

    <body>
        ...
    <body>
</html>
```

## 标题和段落标签

- `<h1>` ~ `<h6>` 用来定义正文文档的标题

  ```html
  <h1> 一级标题 <h1>
  <h2> 二级标题 <h2>
  <h3> 三级标题 <h3>
  <h4> 四级标题 <h4>
  <h5> 五级标题 <h5>
  <h6> 六级标题 <h6>
  ```

- `<p>`定义文本段落，浏览器会自动在段落前后添加空白

  ```html
  <p>这是一个段落</p>
  ```

## 强调标签和注释

  ```html
  <strong>加粗文本</strong>
  <em>斜体文本</em>
  <ins>文本下划线</ins>
  <del>文本删除线</del>
  <mark>高亮文本</mark>
  <small>小号文本</small>
  <span>行内文本容器</span>
  <sub>下标</sub>
  <sup>上标</sup>
  ```

### 链接标签

创建超链接，用于跳转到其他页面、位置或执行操作

```html
<a href="https://example.com" target="_blank" rel="noopener" 
   id="link1" class="external-link" title="访问示例网站"
   download="filename.pdf">
   链接文本
</a>
```

### 引用标签

```html
<blockquote>块级引用</blockquote>
<q>行内引用</q>
<cite>引用标题</cite>
```

## 图片标签以及常见格式

在文档中嵌入图像，必须提供替代文本`alt=""`

```html
<img src="" alt="">
```

图片常用的属性还有：

- `width`：设置图片的宽
- `htight`：设置图片的高
- `title`：鼠标悬停提示

在宽高只定义其中一个时，另外一个会等比例缩放，不过以上属性还是建议使用CSS修改


## 视频和音频标签

### `<video>` 视频标签

```html
<video src=""></video>

<!-- controls：显示播放控件 -->
<video src="video.mp4" controls="controls" width="300"></video>
<!-- h5中如果属性的键和值是相同的，则可以省略值 -->
<video src="video.mp4" controls width="300"></video>
```

视频标签的其他属性:

- `muted`：静音
- `autoplay`：自动播放
- `loop`：循环播放
- `poster`：预览图像

> 由于浏览器的设置，我们想要自动播放需要先设置静音

- 视频标签兼容性写法

  ```html
  <video controls>
    <source src="video.mp4" type="video/mp4">
    <source src="video.ogg" type="video/ogg">
    <source src="video.webm" type="video/webm">
    <p>您的浏览器不支持h5标签，请升级浏览器</p>
  </video>
  ```

  1. 将 `src` 属性放在几个单独的 `<source>` 元素中，这些元素分别指向各自的资源。
  2. 浏览器会检查`<source>`元素，并且播放第一个与其自身相匹配的媒体
  3. 浏览器会根据每个`<source>`的`type`属性来迅速地跳过那些不支持的格式。

- 视频标签开发写法

  oppo、vivo方案
  ```html
  <video src="video.mp4" poster=""></video>
  ```

  小米、京东方案
  ```html
  <video autoplay poster="">
    <source src="video.mp4" type="video/mp4">
    <p>您的浏览器不支持h5标签，请升级浏览器</p>
  </video>
  ```
### `<audio>` 音频标签


`<audio>`标签的写法和 `<video>` 是一样的

但是，浏览器禁止音频标签自动播放，不过可以通过js实现

```html
<audio src="audio.mp3" controls></audio>

<audio controls>
  <source src="audio.mp3" type="audio/mp3">
  <p>您的浏览器不支持h5标签，请升级浏览器</p>
</audio>
<!-- h5中如果属性的键和值是相同的，则可以省略值 -->
<audio src="video.mp4" controls width="300"></audio>
```


### 有序列表

创建有顺序编号的项目列表

```html
<ol id="steps" class="procedure" type="1" start="1" reversed>
  <li>第一步</li>
  <li>第二步</li>
</ol>
```

`<ol>`标签的`type`属性表示排序时使用的标号：

- `"1"` - 使用数字符号（1，2，3）
- `"a"` - 使用小写字母标号（a，b，c）
- `"A"` - 使用大写字母标号（A，B，C）
- `"i"` - 使用小写罗马数字标号（i，ii，iii）
- `"I"` - 使用大写罗马数字标号（I，II，III）

### 无序列表

创建没有特定顺序的项目列表

```html
<ul id="nav-list" class="menu" type="disc">
  <li id="item1" class="menu-item">列表项1</li>
  <li id="item2" class="menu-item">列表项2</li>
</ul>
```

`<ol>`标签的`type`属性表示列表标号：

- `"disc"` - 实心圆（默认）
- `"circle"` - 空心圆
- `"square"` - 小方块
- `"none"` - 不显示

### 定义列表

创建术语及其定义的列表

```html
<dl id="glossary" class="definition-list">
  <dt id="term1">HTML</dt>
  <dd>超文本标记语言</dd>
  <dt id="term2">CSS</dt>
  <dd>层叠样式表</dd>
</dl>
```

### 表格

完整表格结构：

```html
<table id="student-table" class="data-table" border="1">
  <!-- 表格标题 -->
  <caption>学生成绩表</caption>
  
  <!-- 表头 -->
  <thead>
    <tr>
      <th>姓名</th>
      <th>年龄</th>
      <th>成绩</th>
    </tr>
  </thead>
  
  <!-- 表格主体 -->
  <tbody>
    <tr>
      <td>张三</td>
      <td>20</td>
      <td>85</td>
    </tr>
    <tr>
      <td>李四</td>
      <td>19</td>
      <td>92</td>
    </tr>
  </tbody>
  
  <!-- 表脚 -->
  <tfoot>
    <tr>
      <td>平均</td>
      <td>19.5</td>
      <td>88.5</td>
    </tr>
  </tfoot>
</table>

```

#### `<table>` 标签

定义表格的容器

属性：

- `border` - 表格边框宽度

- `cellpadding` - 单元格内边距

- `cellspacing` - 单元格间距

- `width` - 表格宽度

- `height` - 表格高度

- `align` - 表格对齐方式 (`left`|`center`|`right`)

- `bgcolor` - 表格背景颜色

- `summary` - 表格摘要（用于可访问性）

#### `<caption>` 标签

定义表格标题

属性：

- `align` - 标题对齐方式 (`top`|`bottom`)

```html
<caption align="top">2023年学生成绩统计表</caption>
```

#### `<thead>` 标签

定义表格的表头部分，包含列标题

属性：

- `align` - 水平对齐

- `valign` - 垂直对齐

- `bgcolor` - 背景颜色


#### `<tbody>` 标签

定义表格的主体内容

属性：

- `align` - 水平对齐

- `valign` - 垂直对齐

#### `<tfoot>` 标签

定义表格的页脚，通常包含汇总信息

```html
<tfoot>
  <!-- 汇总行 -->
</tfoot>
```

#### `<th>` 标签

定义表头单元格，通常用于列标题

属性：

- `scope` - 关联范围 (`col`|`row`|`colgroup`|`rowgroup`)

- `colspan` - 横跨列数

- `rowspan` - 横跨行数

- `abbr` - 缩写形式

- `headers` - 关联的表头ID

- `align` - 水平对齐

- `valign` - 垂直对齐

- `width` - 单元格宽度

- `height` - 单元格高度

- `bgcolor` - 背景颜色

#### `<tr>`标签

定义表格中的行

属性：

- `align` - 行内单元格水平对齐

- `valign` - 行内单元格垂直对齐

- `bgcolor` - 行背景颜色

- `height` - 行高度

#### `<td>` 标签

定义标准单元格

属性：

- `colspan` - 横跨列数

- `rowspan` - 横跨行数

- `headers` - 关联的表头ID

- `align` - 水平对齐

- `valign` - 垂直对齐

- `width` - 单元格宽度

- `height` - 单元格高度

- `bgcolor` - 背景颜色

- `nowrap` - 禁止换行


#### 列分组 `<colgroup>` 和 `<col>`

对列进行分组，便于统一样式

```html
<table>
  <colgroup>
    <col span="2" style="background-color: #f9f9f9">
    <col style="background-color: #e6f7ff">
  </colgroup>
  <thead>
    <tr>
      <th>姓名</th>
      <th>年龄</th>
      <th>成绩</th>
    </tr>
  </thead>
  <!-- 表格内容 -->
</table>
```

### 表单

#### `<form>`标签

创建包含表单元素的容器，用于收集用户输入

```html
<form action="/submit" method="post" enctype="multipart/form-data"
      autocomplete="on" target="_self" novalidate name="userForm">
  <!-- 表单元素 -->
</form>
```


属性：

- `action` - 表单提交的服务器地址

- `method` - 提交方法 (`get`|`post`)

- `target` - 响应打开位置 (`_blank`|`_self`|`_parent`|`_top`)

- `enctype` - 编码类型 (`application/x-www-form-urlencoded`|`multipart/form-data`|`text/plain`)

- `autocomplete` - 自动完成 (`on`|`off`)

- `novalidate` - 不验证表单

- `name` - 表单名称

- `accept-charset` - 字符编码


#### 表单元素-文本框

文本域通过 `<input type="text">` 标签来设定，当用户要在表单中键入字母、数字等内容时使用

```html
<form>
    First name: <input typt="text", name="firstname">
    <br>
    Last name: <input typt="text", name="lastname">
</form>
```

#### 表单元素-密码框

文本域通过 `<input type="password">` 标签来设定

```html
<form>
    Password: <input typt="password", name="pwd">
</form>
```

#### 表单元素-提交按钮

表单的`action`属性定义了目标文件名。当用户点击确认按钮时，表单的内容会被传送到目标文件，在这个目标文件中进行数据处理。

```html
<form name="input", action="url", method="get">
    Username: <input type="text" name="user">
    <input type="submit", value="登录">
</form>
```

## 交互组件

### 按钮

按钮是用户触发操作的主要交互元素，用于提交表单、执行命令等。

```html
<button type="button" onclick="alert('按钮被点击了!')">点击我</button>
<button type="submit">提交表单</button>
<button type="reset">重置</button>
```

常用属性说明:

- `type` - 按钮类型：`button`(普通)、`submit`(提交)、`reset`(重置)

- `disabled` - 禁用按钮

- `autofocus` - 页面加载时自动聚焦

- `form` - 关联的表单ID

- `name` - 按钮名称

- `value` - 按钮值

### 文本输入框

允许用户输入单行文本的基本表单控件。

```html
<label for="username">用户名：</label>
<input type="text" id="username" name="username" placeholder="请输入用户名">
```

常用属性说明
- `type="text"` - 输入类型为文本

- `placeholder` - 提示文本

- `maxlength` - 最大字符数

- `minlength` - 最小字符数

- `required` - 必填字段

- `readonly` - 只读

- `disabled` - 禁用

- `pattern` - 正则验证模式

### 复选框

允许用户从多个选项中选择一个或多个选项。

```html
<label><input type="checkbox" name="hobby" value="reading"> 阅读</label>
<label><input type="checkbox" name="hobby" value="sports" checked> 运动</label>
<label><input type="checkbox" name="hobby" value="music"> 音乐</label>
```

常用属性说明

- `type="checkbox"` - 输入类型为复选框

- `checked` - 默认选中

- `value` - 选中时提交的值

- `name` - 字段名称

- `disabled` - 禁用

### 单选框

允许用户从多个互斥的选项中选择一个选项。

```html
<label><input type="radio" name="gender" value="male" checked> 男</label>
<label><input type="radio" name="gender" value="female"> 女</label>
<label><input type="radio" name="gender" value="other"> 其他</label>
```

常用属性说明

- `type="radio"` - 输入类型为单选框

- `checked` - 默认选中

- `value` - 选中时提交的值

- `name` - 字段名称（相同`name`的单选框为一组）

- `disabled` - 禁用

### 下拉选择框

提供下拉列表供用户选择，可单选或多选。

```html
<label for="country">选择国家：</label>
<select id="country" name="country">
  <option value="">请选择</option>
  <option value="cn" selected>中国</option>
  <option value="us">美国</option>
  <option value="jp">日本</option>
</select>
```

常用属性说明

- `multiple` - 允许多选

- `size` - 可见选项数量

- `required` - 必填

- `disabled` - 禁用

- `autofocus` - 自动聚焦

### 数据列表

与下拉选择框不同的是，数据列表除了在选项中选择外，用户还可以自由输入

```html
<label for="browser">选择浏览器：</label>
<input list="browsers" id="browser" name="browser">
<datalist id="browsers">
  <option value="Chrome">
  <option value="Firefox">
  <option value="Safari">
  <option value="Edge">
</datalist>
```

常用属性说明
- `id` - 与input的list属性关联

- 通过`<option>`定义预定义选项

### 文本域

允许用户输入多行文本的输入控件。

```html
<label for="message">留言：</label>
<textarea id="message" name="message" rows="4" cols="50" 
          placeholder="请输入您的留言..."></textarea>
```


常用属性说明
- `rows` - 可见行数

- `cols` - 可见列数

- `maxlength` - 最大字符数

- `minlength` - 最小字符数

- `placeholder` - 提示文本

- `readonly` - 只读

- `disabled` - 禁用

- `wrap` - 换行方式

### 文件上传

允许用户选择并上传文件。

```html
<label for="avatar">上传头像：</label>
<input type="file" id="avatar" name="avatar" accept="image/*" multiple>
```

常用属性说明

- `type="file"` - 输入类型为文件

- `accept` - 接受的文件类型

- `multiple` - 允许多文件选择

- `capture` - 媒体捕获方式

- `required` - 必填

### 日期和时间选择器

```html
<label for="birthday">生日：</label>
<input type="date" id="birthday" name="birthday" min="1900-01-01">

<label for="meeting">会议时间：</label>
<input type="datetime-local" id="meeting" name="meeting">
```

常用属性说明
- `type="date"` - 日期选择器

- `type="time"` - 时间选择器

- `type="datetime-local"` - 本地日期时间选择器

- `min` - 最小日期/时间

- `max` - 最大日期/时间

- `step` - 时间步长


### 范围滑块

允许用户通过拖动滑块选择数值范围。

```html
<label for="volume">音量：</label>
<input type="range" id="volume" name="volume" min="0" max="100" value="50" step="10">
```

常用属性说明

- `type="range"` - 输入类型为范围滑块

- `min` - 最小值

- `max` - 最大值

- `step` - 步长

- `value` - 默认值

### 进度条

显示任务完成进度。

```html
<label for="file">文件上传进度：</label>
<progress id="file" value="70" max="100">70%</progress>
```

常用属性说明
- `value` - 当前进度值

- `max` - 最大值

## 块级元素和行内元素（内联元素）

### 块级元素

块级元素在页面中以块的形式展现，通常会独占一行，宽度默认填满父容器，可以设置宽度、高度、内外边距等属性。

**主要特性**

- 默认宽度为100%（占满父容器）

- 总是从新行开始

- 可以包含其他块级元素和行内元素

- 可以设置宽度、高度、上下内外边距

**常见块级元素**

`<div>`，`<h1>`~`<h6>`，`<p>`，`<ul>`，`<ol>`，`<li>`，`<table>`，`<form>`，`<header>`，`<footer>`，`<section>`，`<article>`，`<aside>`，`<nav>`，`<hr>`，`<blockquote>`

### 内联元素（行内元素）

行内元素不会独占一行，只占据内容所需的宽度，多个行内元素可以在同一行内显示。

**主要特性**
- 宽度由内容决定
- 不会强制换行
- 只能包含文本和其他行内元素
- 设置宽度、高度无效
- 上下外边距无效

**常见行内元素**

`<span>`，`<a>`，`<strong>`，`<em>`，`<img>`，`<input>`，`<button>`，`<label>`，`<code>`，`<sup>`, `<sub>`，`<br>`


> 但是元素分类不是一直不可更改的，CSS可以通过`display`来改变他们的显示模式